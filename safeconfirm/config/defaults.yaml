mode: active
policy_backend: rule_v1
risk_threshold_confirm: 0.5
enable_repair: true
registry_path: safeconfirm/data/tool_slot_registry.yaml
templates_path: safeconfirm/data/confirmation_templates.yaml
simulated_confirmer: llm_user
# conservative_confirm | block — tools missing from slot registry
unknown_tool_policy: conservative_confirm
# Synthetic provenance-label flip rate for sensitivity runs (0 = off)
provenance_flip_rate: 0.0
provenance_flip_seed: 0
